/// Type representing an empty closure
typealias VoidHandler = () -> Void

/// Constants specifying configurable values for the project
enum Constants {
    /// Specifies the number of journeys the user can modify (default is 4; as per the current problem statement)
    static var numberOfJourneys = 4
}
