/// A type to represent a M x N grid
struct GridSize {
    /// Represents the number of rows in the grid
    let rows: Int
    
    /// Represents the number of columns in the grid
    let columns: Int
}
